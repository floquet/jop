This file is ${SACHOME}/doc/exmples/interpolate/README

In that directory, issue the command:

./run_interpolate.sh

It will:

1. Compile interpolate
2. Run ./interpolate
3. Do SAC interpolate
4. Check if created SAC files are the same as in ./sample-run using sacdiff -q
